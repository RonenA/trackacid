{{#each songs}}
  {{> songs/_song }}
{{else}}
  <h2 class="l-center-text song-list__empty {{#if favorites}} icon-heart {{else}} icon-music {{/if}}">
    {{{songsEmptyMessage this}}}
  </h2>
{{/each}}

<div class="song-list__loading">
  <i class="icon-spinner animate-spin"></i> Loading
</div>